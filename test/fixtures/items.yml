abc:
  identificador: "ITEM-XYZ"
  group: asc       # referencia al fixture groups.yml (clave `asc`)
  principal: one   # referencia a principals.yml (clave `one`)
  created_at: <%= Time.now %>
  updated_at: <%= Time.now %>
